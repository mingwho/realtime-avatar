# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
venv_gpu/  # GPU virtual environment
env/
ENV/
.venv

# Runtime GPU service files
runtime/SadTalker/  # Cloned SadTalker repository
runtime/assets/  # Duplicate assets (use root-level assets/)
runtime/venv_gpu/  # GPU virtual environment directory
/runtime/models/sadtalker_model.py  # GPU-specific model wrapper
/runtime/models/avatar_client.py  # GPU service client
/runtime/patch_sadtalker.py  # Patching script
/runtime/start_gpu_simple.sh  # GPU startup script

# Original source files (large binaries, converted versions in assets/)
originals/*.heic
originals/*.png
originals/*.jpg

# PyTorch / Model caches
.cache/
*.pth
*.ckpt
*.safetensors

# Output files
/runtime/outputs/
*.wav
*.mp3

# Evaluator outputs - ignore videos but keep JSON test results
evaluator/outputs/*.mp4
evaluator/outputs/**/*.mp4

# GPU service output directory
/tmp/gpu-service-output/

# Test videos (at root)
/*_video.mp4

# Original reference videos (large files)
/originals/*.mp4
/assets/videos/*.mp4

# Local downloads from GCP instances (for spot checking)
downloads/

# Environment
.env
.env.local

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore

# Logs
*.log
logs/

# GCP remote outputs (for spot-checking) - but keep README
outputs/gcp_*/*.wav
outputs/gcp_*/*.mp4
outputs/gcp_*/*.json

# Terraform
infrastructure/.terraform/
infrastructure/*.tfstate
infrastructure/*.tfstate.backup
infrastructure/.terraform.lock.hcl
infrastructure/terraform.tfvars

# Node (for web UI)
node_modules/
dist/
build/
.npm
.eslintcache
